我需要实现或者找到一个部署工具，完全满足下面的需求：

1. 支持多环境进行部署。
2. 同时支持部署到容器，或者云平台。特别是支持国内的云平台
3. 应用中的每个中间件（例如消息队列、数据库等）都可以单独选择是使用云服务，还是安装到容器里。
4. 可以只部署中间件，应用允许用户自己单独启动。在本地开发时中间件默认就部署到基于 docker desktop 的 k8s 即可。
5. 提供更简洁的抽象定义，可以像描述应用的包依赖一样简单描述依赖的中间件。不需要一开始就关注中间件使用的资源等信息。

你来帮我找到最佳的方案，不要考虑当前项目的情况。如果没有完全满足的，那么告诉是否有合适的，可以基于它去开发的工具。